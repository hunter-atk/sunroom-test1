import * as React from 'react';

// components
import { MainTemplate } from '../index'

// styles
import './View3.sass';

export const View3: React.FC = () => {
  
  return (
    <MainTemplate>
      View 3
    </MainTemplate>
  );
};
