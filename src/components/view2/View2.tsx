import * as React from 'react';

// components
import { MainTemplate } from '../index'

// styles
import './View2.sass';

export const View2: React.FC = () => {
  
  return (
    <MainTemplate>
      View 2
    </MainTemplate>
  );
};
